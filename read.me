This repo is a list of models that generates the same text. The voices generated are stored in a subfolder. They will be later on used as a material to test the pitch&SPL modulization performance of a given TTS model.

Model lists:

1. Merlin